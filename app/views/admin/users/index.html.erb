<div class="container">
    <div class="row">
        <div class="col">
            <% @users.each do |user| %>
                <%= link_to admin_user_path(user.id) do %>
                    <%= user.name %>
                <% end %>
            <% end %>
      </div>
  </div>
</div>
