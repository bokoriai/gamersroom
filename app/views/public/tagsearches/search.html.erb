<div class="container">
    <div class="child">
        <% if @word.present? %>
            <h2>"<%= @word %>"タグが付いているスレッド一覧</h2>
            <%= render 'public/boards/show', boardthreads: @boardthreads %>
        <% else %>
            <p class="text-center">スレッドは存在しません</p>
        <% end %>
    </div>
</div>
