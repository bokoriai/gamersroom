<div class="p-3 mb-2 bg-primary text-white">掲示板新規作成</div>
<br>
<%= form_with model: @board, url: public_boards_path, method: :post, local: true do |f| %>
    <%= f.label :title, "作成掲示板タイトル" %>
    <br>
    <%= f.text_field :title %>
    <%= f.submit '掲示板作成', class: 'btn btn-primary' %>
<% end %>